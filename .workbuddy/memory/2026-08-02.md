# 2026-08-02 工作日志

## 代码审查反馈修复（两轮）

### 第一轮：索引/async/幂等三问题
- retriever.py 重写：index_vault 改幂等（delete_collection + 重建 + 写 manifest）；新增 ensure_index_fresh（mtime manifest 增量索引），接入 query_kb / trigger_correlation 子图入口，解决"promote 后不手动跑 index 就检索不到"
- vault_io.create_staged_note 改 async（修复 is_running() 死代码，CLI 优先路径现可真正生效）
- 发现 langchain-text-splitters 未在 pyproject 声明（retriever 运行依赖），补声明并装 venv
- 新增 2 个回归测试（FakeEmbeddings 绕开 ollama 验证幂等 + 增量）

### 第二轮：vault 硬编码 + yakitrak 依赖注释
- 新建 core/tools/vault_resolver.py：resolve_vault_path() 按 obsidian skill 规范动态解析（obsidian-cli print-default → obsidian.json open:true → None）
- settings.py：vault_root 默认去硬编码改空串，加 vault_autodiscover 开关，vault_path property 优先动态解析 fallback .env；测试用 vault_autodiscover=false 隔离避免读到真实 vault
- 真实验证：本机动态解析到 D:\Obsidian\User Data Library\Computer Study（Obsidian 真实注册的 vault），而非之前硬编码的 User Data Library 根 —— 反证硬编码错误
- obsidian_cli.py 修正 yakitrak "不依赖 Obsidian 运行"误导：create/move/open 走 obsidian:// URI 需 Obsidian 桌面端；仅 print-default/search/list 独立于 Obsidian
- 新增 5 个 vault 解析测试；全 19 项测试通过

## 待办（用户后续提供）
- 第 4 点 prompt vs skill 对照：等用户给 Obsidian 库具体结构，核对 skill.md 笔记组织规范与 config/prompts.py 的 INGESTION_SYSTEM/USER 是否一致

## 交付
- 生成详细开发日志到 vault 根目录：`Computer Study/2026-08-02 personal-kb-multiagent 开发日志.md`（含两轮 5 个问题的现象/根因/影响/解决方法/代码要点/测试/经验总结）
